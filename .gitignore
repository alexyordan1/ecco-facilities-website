# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (chat-backend)
node_modules/
.env
.env.local

# Claude
.claude/

# Original/backup files (keep in repo for reference, remove when ready)
# *.original.*

# Alina Stage redesign temporary files (delete after live verification)
*.backup.css
*.backup.js
*.backup.html
mockups/
mockups.html

# Logs
*.log
.superpowers/
.mcp.json

# Pre-minify sources are generated artifacts (not tracked)...
css/*.source.css
# ...except the Noir master: noir.css is minified FROM this file, so the
# source is NOT regenerable and must be tracked (edit source -> minify -> bump).
!css/noir.source.css

# AYS capture artifacts (local reference, not in repo)
docs/superpowers/plans/before/
docs/superpowers/plans/after/
docs/superpowers/plans/mid-*/
docs/superpowers/plans/capture-*.mjs
docs/claude-design-input/
test-results/
playwright-report/
bun.lock
.dead-archive/
_redesign/
